package routes

import "github.com/gin-gonic/gin"

func RegisterInventoryRoutes(api *gin.RouterGroup, deps Deps) {
	// TODO: cuando implementemos inventario, crea el group acá
	// inventory := api.Group("/inventory")
	// {
	// }
}
